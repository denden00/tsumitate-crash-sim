# CLAUDE.md

このファイルは、このリポジトリでClaudeCodeが作業する際のプロジェクトガイドです。実装判断に迷ったら、まずこのファイルと `docs/spec.md`（仕様書）を参照してください。

---

## プロジェクト概要

積立投資シミュレーションに「暴落シナリオ」を組み込んだ特化型ツールサイト。証券会社の標準積立シミュレーターにはない「暴落が来たらどうなるか」を、実際の過去の暴落データに基づいて可視化することがコア価値。

詳細な背景・競合分析・機能要件は `docs/spec.md` を参照。**このファイルとspec.mdで内容が食い違う場合は、より詳細なspec.mdを正とする。**

---

## 技術スタック（確定）

- **フロントエンド**: 素のJavaScript（フレームワーク・ビルドツールは使用しない）
- **ホスティング**: GitHub Pages
- **アーキテクチャ**: 完全な静的サイト。サーバーサイド処理・APIサーバーは持たない
- **データ**: 過去の指数データは事前取得してJSON化し、静的ファイルとしてサイトに同梱する（実行時に外部APIを叩かない）

このスタックを選んだ理由は「学習コストを抑え、GitHub Pagesにそのままデプロイできるシンプルな構成にするため」。新しいライブラリやビルドツールの導入を提案する場合は、まずこの方針と矛盾しないか確認すること。

---

## ディレクトリ構成（想定）

```
/
├── CLAUDE.md              # このファイル
├── index.html             # メインページ
├── style.css              # スタイル
├── main.js                # エントリーポイント
├── js/
│   ├── simulation.js      # 積立・暴落計算ロジック（コア）
│   ├── ui.js              # 拠出プラン／暴落シナリオの編集UI、結果描画
│   └── chart.js           # 資産推移グラフのSVG描画（ライブラリ不使用）
├── data/
│   ├── events.json        # イベント×指数の実データ（下落率・下落期間・回復期間・月次騰落率）
│   ├── index-stats.json   # 指数ごとの年平均リターン（想定利回り欄の参考値）
│   └── fetch-data/        # データ取得・検証用スクリプト（Node.js、サイト本体には含めない）
├── test/
│   └── simulation.test.js # 計算ロジックの検証（node test/simulation.test.js で実行）
└── docs/
    └── spec.md             # 仕様書（正）
```

**グラフ描画は外部ライブラリを使わず、`js/chart.js` で素のJSからSVGを組み立てる方式に確定した（2026-08-25）。**
Chart.js等のCDN読み込みは不採用。データ点は最大でも約360点で、SVGで十分に扱える規模のため。

配色は data-viz 用に検証済みのパレット（`style.css` の `--series-1` / `--series-2`）を使用している。
ライト・ダーク両モードで色覚多様性（CVD）分離とコントラストの検証を通した組み合わせなので、
**系列色を変更する場合は必ず再検証すること。**

その他まだ確定していない部分は、着手前にどう進めるか一度提案してから実装すること。

---

## 計算ロジック（最重要・仕様書4.2/4.3の要約）

積立資産の推移は、期間によって計算方法が変わる。**ここは仕様の核なので、実装時に必ず正確に反映すること。**

1. **通常期間**（暴落発生前 ／ 実データでの回復完了後）
   → ユーザー入力の想定利回り（年率）で複利計算

2. **暴落発生〜回復完了までの期間**
   → 想定利回りは使わず、選択した暴落イベント・指数の**実際の月次騰落率**をそのまま資産推移に反映する
   → 積立は止めない前提。この期間も毎月拠出を継続し、実際の価格水準で口数を買い増す（ドルコスト平均法効果を実データで再現）

3. **「回復完了」の判定基準**
   → 選択した指数が、暴落前の高値を上回った月をもって回復完了とする（資産評価額や元本を基準にしない）

4. **データ粒度**
   → 月次で確定（日次は使わない）

5. **暴落は複数回置ける**（2026-08-26 追加）
   → `input.crashes` に `{ startYear, data }` を並べる。最大5件までUIから追加できる
   → **期間が重なる指定はスキップする。** 前の暴落が高値を回復し切る前に次を置く指定は、
     実データを2つ重ねて適用する妥当な方法が無いため計算から除き、`summary` ではなく
     `result.skippedCrashes` に理由（`overlap` / `out_of_range`）を返してUIで通知する

6. **拠出プラン（積立／停止／取り崩し）**（2026-08-26 追加）
   → `input.phases` に `{ startYear, mode, monthlyAmount }` を並べる。開始年でソートし、
     次のフェーズが始まるまでその内容が続く
   → **取り崩しは「負の拠出」として扱う。** 暴落期間中でもその月の実際の価格で売却する
   → 資産を超える取り崩しはできない。尽きた月を `summary.depletedMonthIndex` に記録し、
     以降は出せる分だけになる（残高はマイナスにしない）
   → 「元本」は**純拠出額（初期元本 + 累計拠出 − 累計取り崩し）**。取り崩しが進むとマイナスにもなる

**「継続 vs 停止」の比較機能は実装しない。** 停止・取り崩しは拠出プランとして期間ごとに指定できるが、
「同じ条件で継続した場合との2本立て比較」は作らない（比較軸は「暴落あり／なし」のみ）。

---

## 差別化要素・スコープ（確定）

- **採用（MVP）**: 複数暴落イベント切り替え機能（実データによる暴落・回復の再現）
- **採用（2026-08-26 追加・ユーザー指示）**: 暴落の複数回配置、拠出プラン（積立／停止／取り崩し）、
  指数別の年平均リターン参考表示、暴落規模の定量表示
- **見送り**: 積立「継続 vs 停止」比較、暴落確率×回復期間の分布機能、シェア機能（すべてv2以降 or 不採用）

MVPスコープ外の機能を提案・実装する場合は、必ず先に確認を取ること。

---

## UIの約束ごと

- **金額の入力欄はすべて「万円」単位。** 内部計算は円なので、`js/ui.js` の境界で ×10000 して
  `js/simulation.js` に渡す。ロジック側に万円を持ち込まないこと
- **暴落イベントの選択肢には、選択中の指数での下落率と回復期間を必ず添える**
  （`Simulation.describeCombination()` が算出する。月次終値ベース）
- **指数の選択が先、イベントの選択が後。** 下落率は指数によって変わるため、指数を決めないと
  イベントの規模を出せない。指数を変えるとイベントの選択肢はその指数で実データがあるものだけに絞られる
- 結果セクションはグラフが最上部。数値サマリー・内訳はその下に置く

---

## 対象イベント・指数（確定・計10×7）

**イベント（10件）**
ITバブル崩壊／リーマンショック／コロナショック／トランプショック／ブラックマンデー／アジア通貨危機／ロシア危機・LTCM破綻／チャイナショック／VIXショック／SVB破綻・米地銀危機

**指数（7件）**
S&P500／TOPIX／日経平均／オルカン相当（ACWI）／NASDAQ100／MSCIエマージング／MSCIコクサイ（除く日本の先進国）

※全組み合わせ（最大70通り）のうち、データ取得できないものは間引く前提。実際に採用する組み合わせは `data/events.json` を正とする。

**現状: 70通り中52件を採用済み。** 除外された18件は、代用ETF（TOPIX=1348.T、MSCIコクサイ=TOK、ACWI、MSCIエマージング=EEM）の設定日より前に発生したイベントとの組み合わせで、データが存在しないため。

---

## データソース・取得方針

- **Yahoo Finance**をCSV取得元として使用
  - 当初Stooqを想定していたが、CSVエンドポイントが自動・手動どちらでも`Access denied`となり不採用に変更した（2026-08-25）
  - Yahoo Financeの内部chart API（`query1.finance.yahoo.com/v8/finance/chart/<ticker>`）が
    認証なしで月次データを返すことを確認し、`data/fetch-data/fetch.js` で自動取得している
    （ブラウザ手動DL用の `generate-urls.js` はフォールバックとして残置）
- Yahoo Financeに公式APIはなく、将来ブロックされる可能性はある
  → **実行時に毎回取得しない。事前に取得してJSON化し、リポジトリに同梱する**方針は変えない
- データ取得・整形スクリプトは `data/fetch-data/` 配下にNode.jsスクリプトとして置き、サイト本体（デプロイ対象）には含めない
- 取得できなかったイベント×指数の組み合わせ（採用ETFの設定日より前のイベントなど）は
  `docs/spec.md` の対象リストから外す（このファイルも合わせて更新する）

### 想定利回りの参考値（`data/index-stats.json`）

`data/fetch-data/fetch-index-stats.js` が指数ごとの全期間データを取得し、CAGR（年平均リターン）を算出する。
`events.json` とは目的が違うので別ファイル・別スクリプトにしている。

- **配当の扱いが指数ごとに違う。** ETFで代用している指数（TOPIX/ACWI/EEM/TOK）は
  調整後終値なので分配金込み、指数そのもの（S&P500・日経平均・NASDAQ100）は配当を含まない価格リターン。
  各要素の `dividendsIncluded` に真偽値を持たせてあるので、**数字を並べて見せるときは必ず注記すること**
- **現地通貨ベース。為替の影響は含まない**（米国指数を円換算していない）
- 取得できる期間は指数ごとに大きく違う（S&P500は1985年〜、TOPIX代用ETFは2009年〜）。
  **CAGRを見せるときは対象期間も必ず併記すること**

---

## マネタイズ・非機能要件（実装時に意識すること）

- **広告（Google AdSense等）がメイン収益源**の想定。ページ設計は広告枠を確保できる構成にする（コンテンツの厚み・記事型ページ構成を意識）
- **免責事項の表示は必須**。「投資助言ではない」旨をフッターに常時表示し、シミュレーション結果ページにも明記すること。これを省略した実装は完了とみなさない
- 過去データの引用元表記（Yahoo Finance等の利用規約に沿った形）を用意する。フッターに記載済み

---

## 開発の進め方（ClaudeCodeへの依頼順）

1. ~~データ取得・検証スクリプト（`data/fetch-data/`）から着手し、10イベント×7指数の実データ取得可否を先に確定させる~~ **完了**
2. ~~取得できたデータを `data/events.json` に整形する~~ **完了（70組み合わせ中52件を採用）**
3. ~~コア計算ロジック（`js/simulation.js`）を実装する~~ **完了（`test/simulation.test.js` で検証済み）**
4. ~~UI（`js/ui.js`、`index.html`、`style.css`）を実装する~~ **完了**
5. ~~免責事項・広告枠等の非機能要件を組み込む~~ **完了（広告枠は `.ad-slot` にプレースホルダーを配置済み。AdSense取得後にコードを差し込む）**
6. ~~複数暴落・拠出プラン（停止／取り崩し）・利回り参考値・暴落規模表示・レイアウト刷新~~ **完了（2026-08-26）**

**UIより先にデータとロジックを固めること。** 手戻りを避けるため、この順序を勝手に入れ替えないこと。

### MVP完了後の残タスク

- GitHub Pagesへのデプロイ設定（リポジトリ作成・Pages有効化）
- AdSense審査・広告コードの挿入（枠は `index.html` の `.ad-slot` に確保済み）
- `data/fetch-data/` をデプロイ対象から除外する設定
- トランプショックなど直近イベントのデータ更新
  （`node data/fetch-data/fetch.js` → `node data/fetch-data/build-events-json.js`。
   利回り参考値は `node data/fetch-data/fetch-index-stats.js` で更新する）

記事セクションのイベント一覧表（下落率・回復期間）は `js/ui.js` が `events.json` から描き直すので、
データを取り直しても手で直す必要はない。イベントの「時期」「特徴」の文言だけ `js/ui.js` の
`EVENT_NOTES` に置いてある。

---

## 迷ったときは

- 機能要件・計算ロジックの詳細 → `docs/spec.md` を参照
- 技術スタック・進め方の方針 → このファイル（CLAUDE.md）を参照
- それでも判断がつかない場合は、実装を進める前に必ず確認を取ること
